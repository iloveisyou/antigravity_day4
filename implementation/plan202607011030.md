# plan202607011030

Gemini API 키 보안 설정 및 연동 계획

## 목표
- `GEMINI_API_KEY` 환경 변수를 안전하게 보관 및 호출
- Vercel Serverless Function을 사용해 클라이언트 브라우저 노출 차단

## 변경 사항
1. `.gitignore`에 `.env` 추가
2. `.env` 파일 생성 (로컬 환경 변수 저장)
3. `api/analyze.js` 신규 생성 (서버리스 함수로 Gemini API 통신 프록시)
4. `app.js`에서 `/api/analyze`로 POST 요청 전송하도록 수정

## 검증 계획
- 로컬 테스트 및 Vercel 배포 후 정상 호출 검증
