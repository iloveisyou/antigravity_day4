# Implementation Plan (2026-07-20 11:38)

## 1. app.js 수정
- `/api/analyze` 요청을 보낼 때 body에 `userId`를 함께 전달합니다.
- `fetchHistory` 함수 호출 시 `/api/history?userId={userId}` 쿼리를 사용하여 해당 유저의 정보만 가져옵니다.

## 2. api/analyze.js 수정
- Body에서 `userId`를 획득합니다.
- Redis 저장 키를 `aiary-{userId}-YYYYMMDDHHmmss`로 변경하여 저장합니다.

## 3. api/history.js 수정
- Query 파라미터에서 `userId`를 획득합니다.
- `redisClient.keys('aiary-{userId}-*')` 형태로 해당 사용자의 키만 매칭하여 조회합니다.
