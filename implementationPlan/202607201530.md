# Implementation Plan (2026-07-20 15:30)

## 1. app.js 수정
- 일기 입력창(`diaryInput`)의 `input` 이벤트 핸들러에서 텍스트가 바뀔 때마다 `localStorage.setItem('diary_text', diaryInput.value)`를 실행하여 실시간 임시 세이브를 작동하게 합니다.
- `analyzeBtn` 클릭 후 감정 분석 성공 처리 시점에 로컬스토리지를 클리어해줍니다:
  - `localStorage.removeItem('diary_text');`
  - `localStorage.removeItem('diary_ai_response');`
- 분석 성공 후 일기 입력창도 함께 비워줍니다:
  - `diaryInput.value = '';`
  - `currentCharCount.textContent = '0';`
