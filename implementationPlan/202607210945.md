# Implementation Plan (2026-07-21 09:45)

## 1. app.js 수정
- `/api/analyze`와 `/api/history`를 호출할 때 `Authorization: Bearer <access_token>` 요청 헤더를 실어 전송하도록 보완합니다.
- Redis의 새로운 키 형식(`user:{userId}:diary-YYYYMMDDHHmmss`)에 맞게 히스토리 날짜를 추출하는 정규식(`/diary-(\d{4}).../`)을 업데이트합니다.

## 2. api/analyze.js 수정
- `api/_lib/supabase.js` 모듈을 임포트하여 헤더에 전달받은 토큰의 검증 과정을 구현하고 `user.id`를 식별합니다.
- Redis 저장 키 구조를 `user:{userId}:diary-YYYYMMDDHHmmss` 규격으로 변경합니다.

## 3. api/history.js 수정
- 동일하게 헤더의 토큰 검증 과정을 적용하여 `user.id`를 추출합니다.
- Redis 키 검색을 `user:{userId}:diary-*` 패턴으로 좁혀서 조회하도록 수정합니다.
- 파일 내부 날짜 파싱 정규식도 마찬가지로 `/diary-(\d{4}).../`로 변경합니다.
